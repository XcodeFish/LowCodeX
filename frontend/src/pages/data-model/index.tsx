import React, { useEffect } from 'react';
import { Navigate } from 'react-router-dom';

const DataModelIndex: React.FC = () => {
  return <Navigate to="/models" replace />;
};

export default DataModelIndex;
